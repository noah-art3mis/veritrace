# Raw academic corpora you download locally (see README) — not redistributed here.
/raw/
*.tsv
*.csv

# Generated golden sets — regenerate with convert.mjs; don't commit large/NC-licensed dumps.
*.jsonl
# ...but DO keep the tiny hand-checked smoke set under version control.
!smoke.jsonl
